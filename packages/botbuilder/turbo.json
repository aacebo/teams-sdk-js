{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "cache": false,
      "dependsOn": [
        "@teams/apps#build",
        "@teams/common#build"
      ]
    }
  }
}
