# Teams: Config

shared configuration files used to keep the other packages consistent.

> ⚠️ **this repository has been moved to https://github.com/microsoft/spark.js** and renamed `@microsoft/spark.config`.

## Install

```bash
$: npm install @teams.sdk/config
```

## Dependencies

N/A
