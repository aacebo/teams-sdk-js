{
  "name": "@teams/api",
  "version": "0.0.0",
  "license": "MIT",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "npx rimraf ./dist",
    "build": "npm run clean && tsc -p ."
  },
  "dependencies": {
    "@teams/common": "*",
    "@teams/config": "*",
    "@teams/schema": "*",
    "qs": "^6.13.0"
  },
  "devDependencies": {
    "@types/qs": "^6.9.15",
    "rimraf": "^6.0.1",
    "typescript": "^5.4.5"
  }
}
