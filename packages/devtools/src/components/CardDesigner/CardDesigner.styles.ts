import { makeStyles } from '@fluentui/react-components';

export const useCardDesignerClasses = makeStyles({
  container: {
    display: 'flex',
    flex: 1,
    minHeight: 0,
  },
  content: {
    flex: 1,
    minHeight: 0,
  },
});
