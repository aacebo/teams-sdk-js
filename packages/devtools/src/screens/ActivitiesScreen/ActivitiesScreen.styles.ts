import { makeStyles } from '@fluentui/react-components';

const useStyles = makeStyles({
  activitiesContainer: {
    display: 'flex',
    flexDirection: 'row',
    flex: 1,
  },
});

export default useStyles;
